spring:
  datasource:

    url: jdbc:h2:mem:test
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  mvc:
    hiddenmethod:
      filter:
        enabled: true
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create
cloudinary:
  cloud-name: dbefqudip
  api-key: 331276785157647
  api-secret: ${CLOUDINARY_API_SECRET}
